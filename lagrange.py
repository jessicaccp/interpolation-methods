def lagrange(x, y):
    pass
    # n = len(x)
    # midpoint = (x[n-1] - x[0]) / 2

    # pn = 0
    # for i in range(n):
    #     lk = 0
    #     for j in range(n):
    #         midpoint

    #     p += ny[k]